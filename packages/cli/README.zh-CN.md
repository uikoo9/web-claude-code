# @webccc/cli

简体中文 | [English](../../README.md)

Web Claude Code CLI - 让你可以在浏览器中使用 Claude Code 的命令行工具。

## 简介

`@webccc/cli` 是一个命令行工具，让你可以直接在浏览器中使用 Claude Code，通过便捷的 Web 终端界面与 Claude CLI 交互。

![演示截图](https://static-small.vincentqiao.com/webcc.png)

功能特性：

- 🚀 快速启动服务，在浏览器中访问 Claude Code
- 🔧 自动检测 `.env` 配置文件
- 💬 交互式配置向导
- 🎨 彩色日志输出
- 📦 开箱即用

## 安装

### 全局安装（推荐）

```bash
npm install -g @webccc/cli
```

全局安装后，可以在任意目录使用 `webcc` 命令。

### 本地安装

```bash
npm install @webccc/cli
```

本地安装后，可以通过 `npx webcc` 或在 `package.json` 脚本中使用。

## 使用方法

### 快速开始

```bash
webcc
```

CLI 会自动检查当前目录是否存在 `.env` 文件：

- **存在 `.env` 文件**：自动加载配置并启动服务器
- **不存在 `.env` 文件**：启动交互式配置向导

### 命令选项

```bash
webcc -h          # 显示帮助信息
webcc --help      # 显示帮助信息
webcc -v          # 显示版本号
webcc --version   # 显示版本号
```

## 配置

### 方式一：使用 .env 文件（推荐）

在项目目录创建 `.env` 文件：

```bash
# Anthropic API 配置（必填）
ANTHROPIC_BASE_URL=http://your-api-url:3000/api
ANTHROPIC_AUTH_TOKEN=your_auth_token_here

# Claude CLI 配置（可选）
CLAUDE_PATH=claude
WORK_DIR=/path/to/your/project

# Claude 模型配置（可选）
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_SMALL_FAST_MODEL=claude-sonnet-4-5-20250929

# 服务器配置（可选）
PORT=4000
HOST=0.0.0.0
```

然后运行：

```bash
webcc
```

### 方式二：交互式配置

如果不存在 `.env` 文件，运行 `webcc` 会提示你输入：

1. Anthropic API Base URL（必填）
2. Anthropic Auth Token（必填）
3. Claude CLI 路径（可选，默认：`claude`）
4. 工作目录（可选，默认：当前目录）
5. Claude 模型（可选，默认：`claude-sonnet-4-5-20250929`��
6. 小型快速模型（可选，默认：`claude-sonnet-4-5-20250929`）
7. 服务器端口（可选，默认：`4000`）
8. 服务器主机（可选，默认：`0.0.0.0`）

## 配置项详情

### 必填配置

| 配置项                 | 环境变量               | 说明                   |
| ---------------------- | ---------------------- | ---------------------- |
| Anthropic API Base URL | `ANTHROPIC_BASE_URL`   | Anthropic API 基础 URL |
| Anthropic Auth Token   | `ANTHROPIC_AUTH_TOKEN` | Anthropic API 认证令牌 |

### 可选配置

| 配置项          | 环境变量                     | 默认值                       | 说明                      |
| --------------- | ---------------------------- | ---------------------------- | ------------------------- |
| Claude CLI 路径 | `CLAUDE_PATH`                | `claude`                     | Claude CLI 可执行文件路径 |
| 工作目录        | `WORK_DIR`                   | 当前目录                     | Claude CLI 工作目录       |
| Claude 模型     | `ANTHROPIC_MODEL`            | `claude-sonnet-4-5-20250929` | 主要使用的 Claude 模型    |
| 小型快速模型    | `ANTHROPIC_SMALL_FAST_MODEL` | `claude-sonnet-4-5-20250929` | 用于快速任务的小型模型    |
| 服务器端口      | `PORT`                       | `4000`                       | Web 服务器监听端口        |
| 服务器主机      | `HOST`                       | `0.0.0.0`                    | Web 服务器监听主机        |

## 使用示例

### 示例 1：使用 .env 文件

```bash
# 创建 .env 文件
cat > .env << EOF
ANTHROPIC_BASE_URL=http://localhost:3000/api
ANTHROPIC_AUTH_TOKEN=sk-ant-xxxxx
PORT=8080
EOF

# 启动服务器
webcc
```

输出：

```
  _    _      _      _____ _____
 | |  | |    | |    /  __ /  __ \
 | |  | | ___| |__  | /  \| /  \/
 | |/\| |/ _ | '_ \ | |   | |
 \  /\  |  __| |_) || \__/| \__/\
  \/  \/ \___|_.__/  \____/\____/

  webcc.dev: web-claude-code
  Version: 0.0.4

✓ [12:34:56] 发现 .env 文件，正在加载配置...
ℹ [12:34:56] 正在启动服务器...

✓ [12:34:56] 服务器启动成功！

ℹ [12:34:56] 访问地址:
ℹ [12:34:56]   本地: http://localhost:8080
ℹ [12:34:56]   网络: http://<your-ip>:8080
ℹ [12:34:56]
按 Ctrl+C 停止服务器
```

### 示例 2：交互式配置

```bash
webcc
```

提示信息：

```
  _    _      _      _____ _____
 | |  | |    | |    /  __ /  __ \
 | |  | | ___| |__  | /  \| /  \/
 | |/\| |/ _ | '_ \ | |   | |
 \  /\  |  __| |_) || \__/| \__/\
  \/  \/ \___|_.__/  \____/\____/

  webcc.dev: web-claude-code
  Version: 0.0.4

ℹ [12:34:56] 未找到 .env 文件，请提供以下配置信息：

? Anthropic API Base URL (必填): http://localhost:3000/api
? Anthropic Auth Token (必填): sk-ant-xxxxx
? Claude CLI 路径: (claude)
? 服务器端口: (4000)
? 服务器主机: (0.0.0.0)

ℹ [12:34:56] 正在启动服务器...
...
```

## 系统要求

- Node.js >= 14.0.0
- 系统已安装 Claude CLI
- expect 工具（用于 PTY 支持）
  - macOS: `brew install expect`
  - Ubuntu/Debian: `sudo apt-get install expect`
  - Windows: 使用 WSL 或替代方案

## 集成到 npm scripts

添加到 `package.json`：

```json
{
  "scripts": {
    "start": "webcc"
  }
}
```

然后使用：

```bash
npm start
```

## 故障排除

### 问题：找不到 Claude CLI

**错误信息：**

```
✖ [12:34:56] 启动 CLI 失败: spawn claude ENOENT
```

**解决方案：**

- 确保已正确安装 Claude CLI
- 检查 `claude` 命令是否在 PATH 中
- 或在 `.env` 中通过 `CLAUDE_PATH` 指定完整路径

### 问题：端口已被占用

**错误信息：**

```
✖ [12:34:56] Error: listen EADDRINUSE :::4000
```

**解决方案：**

- 在 `.env` 中通过 `PORT=8080` 更改端口号
- 或释放被占用的端口

### 问题：缺少 expect 工具

**错误信息：**

```
✖ [12:34:56] 启动 CLI 失败: spawn expect ENOENT
```

**解决方案：**

- macOS: `brew install expect`
- Linux: `sudo apt-get install expect`
- Windows: 使用 WSL

## 许可证

MIT

## 链接

- [GitHub](https://github.com/uikoo9/web-claude-code)
- [问题反馈](https://github.com/uikoo9/web-claude-code/issues)
- [官网](https://webcc.dev)
